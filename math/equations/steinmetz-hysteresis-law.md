# Steinmetz Hysteresis Law

Source status: source-located OCR candidate; scan verification required.

Primary source anchors:

- `Theory and Calculation of Alternating Current Phenomena`, hysteresis chapter OCR around the loss of energy by hysteresis.
- `Theoretical Elements of Electrical Engineering`, hysteresis and effective resistance section OCR around the coefficient of hysteresis and worked examples.
- `Engineering Mathematics`, empirical curve discussion where Steinmetz treats the 1.6-power law as an empirical fit.

Working historical form:

```math
W = \eta B^{1.6}
```

where `W` is energy loss per cycle per unit volume in the historical unit frame, `eta` is the coefficient of hysteresis, and `B` is magnetic density or magnetic induction.

Power form:

```math
P = f\eta B^{1.6}
```

The public tool keeps the exponent and coefficient adjustable because the archive still needs to verify exact source typography, units, and edition-specific notation before promoting a final canonical calculator.
